public class test {

    public final int nine = 2000;

    private int integer 
    public boolean motorOn(boolean motorOn) {
        if (motorOn) {
            randomInt(2, 9);
            return true;
        } else {
            int push = 200;
            
            return false;
        }
    }

    @SuppressWarnings("unused")
    public void explodeAndRun() {
        onExplode();
        runMotor();
        int value = 300;
    }

    private static int randomInt(int bound) {
        return (int) (Math.random() * bound);
    }

    private static int randomInt(int min, int max) {
        return min + (int) (Math.random() * (max - min + 1));
    }

    private void onExplode() {
    
    }

    private void runMotor() {

    }

    //DONT CLOSE THIS WHOEVER IS READING THIS ON FRIDAY!!!!!!!!
}